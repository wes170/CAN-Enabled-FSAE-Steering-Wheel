# Project Log — gate sign-offs and assumption tracker

`memory/engineering-rigor.md` says gates are "signed & dated in the project log" and assumptions are
"tracked, numbered, and closed." This is that log. **Nothing here is ticked by whoever did the work
alone** — G5 exists specifically so a second person looks.

Keep this file honest. A gate marked passed that wasn't is worse than no gate, because it silently
removes the check everyone downstream assumes happened.

---

## 1. Open assumptions (from `memory/system-architecture-and-can.md` §5)

Close these before trusting the design. **A1 and A2 are closable for the price of a ~$30 USB-CAN
sniffer, with no PCB spend** — do them first, because they are the two that could force a respin.
~~A6 must be closed before dash layout~~ — **closed 2026-07: standard PWM servos confirmed**, so the
dash output stage is correct as drawn and layout is unblocked. Only BEC sizing remains under A6.

| # | Assumption | Blocks | How to close | Status | Closed by / date |
|---|---|---|---|---|---|
| A1 | NSP detects an emulated keypad at node `0x15`, 1 Mbit/s | Wheel firmware | Sniff a real Haltech keypad / NSP discovery on bench CAN | **OPEN — deferred by choice** (user, 2026-07): not a blocker for schematic/layout work. Kept as a one-line constant so it is cheap to change | |
| A2 | IO12 **Box B** CAN ID set (Box A verified) | Dash DAQ re-broadcast | Request write protocol from Haltech support (supplied to owners), or sniff a Box B | **OPEN — deferred by choice** (user, 2026-07) | |
| A3 | WS2812B-2020 worst case 36 mA/LED | Wheel power budget | Measure a 24-LED strip at full white | OPEN | |
| A4 | TC / lockup status source on the broadcast bus | Wheel LED firmware | Decide with the tuner in NSP; bind the config table | OPEN | |
| A5 | Riverdi backlight inrush | **Dash 5 V buck** (corrected) | Scope at power-on; verify **LMR36015** soft-start covers it. **Was written against the 3.3 V rail and the AP63203 — wrong since defect 5.3: the backlight sits on BLVDD fed from `+5V` at 353 mA, not on 3.3 V.** The 3.3 V rail never sees this load | OPEN | |
| A6 | ARB servo **stall current** (assumed ~5 A @ 7.4 V/ch) | **BEC sizing only — no longer blocks dash layout** | Get the part number from vehicle dynamics | **PARTIALLY CLOSED** — user confirmed **standard PWM servos** (2026-07), so the dash output stage (TIM4 → 74AHCT2G125 → 100 Ω → SMAJ5.0A) is correct as drawn. Only the harness BEC sizing remains | PWM confirmed 2026-07 |
| A7 | Does the ARB mechanism back-drive on power loss? | ARB safety strategy | Mechanical team. Worm drive holds; direct lever does not | OPEN | |
| A8 | Wheel 12 V transient environment (now seen raw) | Wheel input protection | Scope the feed during crank, alternator steps, fan/solenoid switching | OPEN | |
| A9 | Wheel colour display rated −20…+70 °C, tighter than the rest of the BOM | Wheel display longevity | Measure panel surface temperature on a summer track day; faceplate shade/recess if exceeded | OPEN | |
| A10 | **Wheel `J2` USB-C rated −30…+85 °C**, tighter at the cold end than the −40…+85 °C the rest of the BOM holds | Wheel cold-weather storage/operation | Judgement call, not a measurement: −30 °C is well below any temperature the car is driven at, and the connector is unmated and unloaded in storage. Recorded so the deviation is a decision rather than an oversight — **close it by accepting it in writing**, or swap the connector if the team ever specifies a −40 °C requirement | OPEN — low risk | |

---

## 2. Review gates

Run in order, per board. Evidence means an artifact someone else can re-check (a screenshot, a
report file, a photo, a scope capture) — not a memory of having looked.

### FSAE-WHEEL Rev B

| Gate | Content | Evidence | Passed | Signed / date |
|---|---|---|---|---|
| G1 | Schematic review — ERC clean, suppressions justified, pin maps match docs, power budget re-added, every connector pin accounted for | | ☐ | |
| G2 | Layout review — DRC clean, 3D collision vs. faceplate STEP, L2 GND unbroken, protection-at-connector audit, buck hot-loop audit | | ☐ | |
| G3 | Paper build — 1:1 print on the real wheel, gloved reach test for every encoder and button | | ☐ | |
| G4 | Netlist cross-check — J1 pinout re-derived independently, **including which ground each signal references** (L13) | | ☐ | |
| G5 | Peer sign-off — second person reviews G1–G4 evidence | | ☐ | |
| G6 | Pre-order — BOM availability **and lifecycle** re-checked same-day (rule 3a), **no fabrication output still selects a variant** (Rev B.5 deleted CAR/SIM — `[No Variations]` is now correct), gerbers viewed in a non-Altium viewer | | ☐ | |

### FSAE-DASH Rev B

| Gate | Content | Evidence | Passed | Signed / date |
|---|---|---|---|---|
| G1 | Schematic review (as above). A6's layout risk is closed — PWM servos confirmed | | ☐ | |
| G2 | Layout review (+ LMR36015 hot loop, analog zone separation from the servo PWM traces) | | ☐ | |
| G3 | Paper build — 1:1 print in the real dash panel, FPC fold mocked in paper | | ☐ | |
| G4 | Netlist cross-check — J1 **and** J2 pinouts, and the servo ground reference path | | ☐ | |
| G5 | Peer sign-off | | ☐ | |
| G6 | Pre-order | | ☐ | |

---

## 3. Procurement actions (do these before PCB spend)

| Item | Why it is first | Status |
|---|---|---|
| **Riverdi RVT50HQBNWN00 + 1 spare** | Single-source, thin distributor stock, longest lead in the BOM. Nothing about the PCB design depends on ordering boards first, so there is no reason to wait | ☐ |
| USB-CAN sniffer (~$30) | Closes A1 and A2 without spending board money | ☐ |
| Encoders (PEC09 + PEC11H) | Confirm shaft length against the real faceplate depth before the footprint is frozen | ☐ |
| **JDI LPM013M126A wheel display + 1 spare** | Specialty distributors only (Switch-Science / Data Modul / Youritech), not Digi-Key or LCSC — thinner supply than the rest of the BOM. Sharp LS013B7DH05 is the zero-board-change mono fallback if it fails | ☐ |
| Haltech IO12 Box B write protocol | Email Haltech support with proof of ownership (they supply it) — closes A2 | ☐ |
| **`J2` USB-C — confirm Molex `204711-0001` can actually be assembled** | Changed to a **vertical** part at user request (2026-07) for packaging behind the faceplate. **Molex is a Digi-Key/Mouser line and JLCPCB assembles from LCSC** — if it is not in the LCSC catalogue, `J2` becomes a consigned or hand-fitted part, which is a different assembly order rather than a different BOM line. Check LCSC, and if absent decide consign-vs-hand-solder before the BOM is frozen. ~~Read the Molex product drawing and transcribe the pad map~~ — **DONE**: drawing `2047110001` rev B is in `hardware/lib/`, §3.4 carries the full pad map, and the capture procedure is `wheel-pcb-altium-instructions.md` §3.2a. Only the LCSC/assembly question remains open here | ☐ |
| **`Y1` crystal — confirm `ABM8-16.000MHZ-8-D4Y-T` is orderable** | CL 8 pF and −40…+85 °C are **non-standard option codes** on the ABM8 (standard is CL 18 pF, −10…+60 °C). The combination may carry an MOQ or a factory lead time rather than sitting in distributor stock, and the substitute has to meet ESR ≤ 70 Ω / C0 ≤ 3 pF / CL = 8 pF — *not* the ABM8G. Check stock, MOQ and lead time before the BOM is frozen. **Buy 2 boards' worth + spares in one order** if it is a factory line. Defect 8.14 | ☐ |

---

## 4. Revision history

| Rev | Date | Change |
|---|---|---|
| A | 2026-07 | Initial design. Wheel fed 5 V from the dash |
| B | 2026-07 | Wheel moved to vehicle 12 V and made standalone; dash 5 V AUX deleted and 5 V buck downsized to 2 A; 2× ARB servo outputs added to the dash on J1.5/J1.6; obsolete EVQ-WK4001 thumb encoder replaced with the Bourns PEC09 right-angle, plus a DNP satellite-board option |
| B.1 | 2026-07 | Wheel display changed to colour (JDI LPM013M126A, 176×176 8-colour reflective MIP) — pin-for-pin identical to the Sharp mono part, so a BOM change only; Sharp retained as a zero-board-change fallback. Adds assumption A9 |
| B.2 | 2026-07 | **Step 2 full design pass — nine defects** (`datasheet-verification.md` §9). Board changes: **`D14`/`D15` BAV199 clamps added** to the paddle sense nets (8.7 — the divider alone let a TVS clamp put 8 V on a 4.0 V pin), and both BOMs gained the LMR36015's datasheet-required `C_BOOT`, `C_VCC`, `R_FBT`, `R_FBB`, `C_FF` plus the mandatory 2 × 220 nF VIN bypass caps, which had existed only as prose (8.5); the dash buck's input/output capacitors were still the rejected LMR33630's values; and **`C_BST2`, the AP63203's bootstrap capacitor, was missing entirely** (8.8) — without it the 3.3 V rail never comes up. AP63203 datasheet read in full, closing the `L2` open item (8.8b). Firmware requirements: `PWR_CR3.UCPD1_DBDIS` must be set before any GPIO setup or the debug UART disables `ENC3_A` (8.2), and the wheel rail-monitor ADC channels were off by one (8.3). Documentation: the authoritative §9 pin table was stale (8.1) and both Altium instruction files still named rejected power parts (8.4, 8.6). Adds `scripts/check-consistency.py` as a commit gate. **A follow-up accuracy audit added three more:** every capacitor on the 53.3 V-clamped 12 V rail was rated 50 V (8.10 — four BOM lines on both boards, now 100 V); the LMR36015 EN-pin justification cited a datasheet rule that does not exist (8.11 — the circuit is correct, the evidence was invented); and the buck variant warning pointed the wrong way, saying stock is 1 MHz when LCSC stocks the 400 kHz part (8.12 — a live buying hazard). The wheel's paddle clamps now specify **both** BAV199 halves, since the SMAJ24CA is bidirectional |
| B.3 | 2026-07 | **HSE crystal selected and verified** — Abracon ABM8, 16.000 MHz, **CL 8 pF**, ESR ≤ 70 Ω, C0 ≤ 3 pF, ±30 ppm, −40…+85 °C; `C_X1`/`C_X2` = **6 pF**. The old "8–16 MHz, CL 8–12 pF, ESR ≤ 80 Ω" range permitted startup margins from 6.5× down to 2.1×, so it was never a specification. Two circuit corrections came with it: **`C_X2` moves to the crystal side of `R_X1`** (on the MCU side, `R_X1` costs startup margin and limits no drive at all), and the MCU decoupling count drops from six to **five** — the LQFP-64 has **four** VDD pins, not six, counted off DS12288 Figure 7. Adds a **complete 64-pin connection table** to both schematic definitions (wheel §3.1, dash §8.0), cross-checked against the signal maps and the firmware by `check-consistency.py`. Drive level (117 µW est. vs 100 µW max) and cold start are now explicit bring-up measurements. The plain-English guides were brought up to date with all of the above; doing so surfaced **defect 8.13** — the dash BOM's decoupling note was copied from the wheel's and pointed VDDA at `C8`/`C9`, which on the dash are the AP63203's output capacitors (VDDA is `C10`/`C11` there). `check-consistency.py` now resolves that class of BOM note cross-reference |
| B.4 | 2026-07 | **ABM8 datasheet read first-hand** (rev. 07-29-20, supplied by the user). Every electrical figure the crystal selection rests on confirms exactly — the Table 1 ESR bands (400/120/70/50 Ω at 8/12/16/20 MHz), C0 3.0 pF max, 100 µW drive-level max with 10 µW typical, ±2 ppm first-year aging, and the 3.2 × 2.5 × 0.8 mm outline — so the 4.4× startup margin and the ±91 ppm budget stand unchanged. **Defect 8.14**, found in the ordering information rather than the specifications: `CL` and operating temperature are *order options* on the ABM8, and the defaults are wrong for this design — standard is **CL 18 pF** and **−10…+60 °C**, and CL 18 pF cuts startup margin to **1.2×** (a board that may not oscillate) while also demanding 26 pF load capacitors instead of the 6 pF fitted. The part number is now stated in full as **`ABM8-16.000MHZ-8-D4Y-T`** in both BOMs and every guide, with the two load-bearing options distinguished from the two that are merely preferences. Adds a procurement action: confirm that non-standard combination is orderable |
| B.5 | 2026-07 | **CAR/SIM assembly variants deleted — one build, and the only difference is which cable is plugged in.** `R_VBUS` (the 0 Ω link ORing USB VBUS into `+5V` through `D6`) is now fitted on every board; the CAN transceiver and the 12 V front end are fitted on every board. The USB port itself was always populated on both variants, so this closes the gap that remained: USB as a *power* source. Three defects came out of it. **8.15:** fitting `R_VBUS` alongside the 12 V front end — a combination neither variant had — completes a path from USB VBUS through the LMR36015's high-side body diode onto `+12V_P` at ~4 V, which turns `Q1` on and puts ~4 V on `J1` pin 1. Benign with `J1` unmated; the real case is USB into a board mated to a switched-off car. **Accepted rather than fixed**: the obvious series Schottky costs 0.4 V and the TJA1051T/3 needs 4.75 V minimum, so blocking it would violate the transceiver spec permanently to guard against an occasional annoyance. Mitigated in firmware (vehicle-present threshold 7 V assert / 6 V release, swept across the whole back-feed band in `test_power.c`) and gated at bring-up (§4 step 3b). **8.16:** `sim-variant-instructions.md` promised a DNP `R_CC_SNS` footprint that existed in no schematic and no BOM — removed rather than added, since lifting the LED cap is an optimisation that would need two unverified documents. **8.17:** §3.2's claim to "clock both CAN and USB from the HSE" is arithmetically impossible — 170 MHz SYSCLK forces VCO = 340 MHz while 48 MHz on PLLQ needs VCO ∈ {96,192,288,384}; USB runs on **HSI48 + CRS**, now implemented in `usb_clock_init()` with a static assertion guarding the argument rather than the result. **The USB LED cap drops 350 → 300 mA**: the old figure was correct while the CAN transceiver was DNP on sim boards, and 110 + 70 + 350 = 530 mA does not fit a 500 mA allowance — a build-time constant that went stale when the fitted-list changed, which is the strongest argument for the merge. The cap is now chosen at run time from `V12_SENSE` (hardware that already existed), and is **zero before USB enumeration**. New `firmware/{include/power.h,src/power.c,test/test_power.c}`; `led_show()` reads the live cap every frame. **Defect 8.18** came out of the checker written for 8.16, on its first run: **five components specified in the schematic had no BOM line**, two of them fitted — `C_U6` (the LED buffer's only decoupling cap) and **`R_EXTMODE`**, the 0 Ω strap without which the JDI panel's `EXTMODE` pin floats and, per §7 rule 3, the panel is **permanently damaged**. All five added; the check is per-board so one BOM cannot excuse the other. Adds lessons L47–L51 |
| B.6 | 2026-07 | **Wheel `J2` changed to Molex `204711-0001`, a vertical (board-perpendicular) USB-C receptacle** — user decision for packaging: it exits away from the board instead of needing a clear run to a board edge plus an aligned faceplate hole. Replaces LCSC `C165948` (HRO TYPE-C-31-M-12), which the **dash `J3` still uses**, so the two boards no longer share a USB-C footprint and the shared library must carry both. Three consequences recorded rather than resolved: **sourcing** (Molex is Digi-Key/Mouser, JLC assembles from LCSC — `J2` may be consigned or hand-fitted, and it has no LCSC code or JLC rotation to back-annotate), **z-height** against the faceplate (G2 3D check and G3 paper build, with the specific question of whether a cable can be inserted and removed with the wheel assembled), and **withdrawal force** peeling the pads upward — confirm whether the six mounting tabs are through-hole or SMT. The connector's pad map is `[OPEN]` and **blocks G6**. Also names **`NET_SHIELD`**: the symbol's `MNT 1`–`MNT 6` shell tabs go there and reach `GND` only through `R11` ∥ `C24`, never directly — the net had been referred to as "shield" in prose in a file whose §0 requires literal net names |
| B.7 | 2026-07 | **Molex drawing `2047110001` rev B read and transcribed** (saved to `hardware/lib/`). `J2`'s pad map is **closed** and §3.4 now carries it verbatim, with a full capture procedure at `wheel-pcb-altium-instructions.md` §3.2a. Three corrections came with it. **(a) The part is 24-position USB Type-C, not the "USB-C 16-pin" every document said** — functionally fine (USB 2.0 signalling on a 24-pos connector is normal), but the footprint is different and 10 pads are now unused: the four SuperSpeed pairs and SBU1/SBU2, which must be left **floating**, never grounded, because a full-featured cable drives them live. **(b) Withdrawal force is closed, not merely mitigated** — all six anchors are **through-hole** with gauge-controlled tails, so pull-out load goes into barrels rather than peeling SMT pads. **(c) The six `MNT` tabs are two structures, not one** — the drawing labels `SHELL` and `MIDDLE BLADE` separately (4 corner slots + 2 mid-height) and does **not** state they are electrically common; all six go to `NET_SHIELD`, which is correct either way, and a meter across a sample settles it. Height is **9.80 mm** for the G2/G3 faceplate check. Adds **A10**: the connector is rated −30…+85 °C, tighter at the cold end than the −40…+85 °C the rest of the BOM holds — low risk, recorded so it is a decision rather than an oversight |
| B.8 | 2026-07 | **BAV199 data sheet read first-hand** (Nexperia, 1 April 2023, saved to `hardware/lib/`), prompted by the user asking why a three-pin part had only two pins described. **Defect 8.19:** every document specified the clamp *electrically* — "upper diode anode → net, lower diode cathode → net" — and **none gave a physical pin number**; worse, that phrasing implies the net connects twice, when the BAV199's diodes are **in series** and meet on pin 3. Canonical mapping now in both schematic definitions, both BOMs and both guides: **pin 1 `A1` → `GND`, pin 2 `K2` → `+3V3`, pin 3 `K1,A2` → the sense net.** Added the non-interchangeability warning — BAV70 (common cathode), BAW56 (common anode) and BAV99/BAV199 (series) share a package and a symbol, and only the series arrangement can clamp both rails from one net. **Defect 8.20:** the wheel plain-English guide never received the 8.7 correction and still described only the upper clamp, three files updated and the fourth missed — the guides restate rather than duplicate, so a literal search for the old text does not find them. Also flagged for G1: §4.3 justifies the lower diode as holding the pin above VSS − 0.3 V, which a silicon diode cannot do (it clamps near −0.7 V); the part is right, the sentence needs rewriting against DS12288 Tables 14/15 — the real benefit is diverting current away from the MCU's ESD structure. `check-consistency.py` now fails if a multi-terminal part is described without its pin numbers, and if the wheel guide describes the clamp without both halves. Adds lessons L52–L53 |
